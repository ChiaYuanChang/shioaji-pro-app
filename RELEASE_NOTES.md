## v0.1.13 — 小線圖、通知中心、診斷面板

### 自選清單小線圖
- 每列名稱與價格之間顯示當日迷你走勢線（canvas 繪製，行情即時連動、跟隨欄寬）
- 顏色依相對昨收的漲跌，昨收畫為虛線基準
- 可開關：選單列快捷面板 ⚙「小線圖」（預設開）、主視窗自選清單 📈 按鈕（預設關），偏好會記住

### 通知中心（新面板）
- toast 會消失，通知中心保留完整歷史：所有 App 通知與委託／成交回報
- 全部／成功／錯誤／訊息篩選、一鍵清除、時間戳

### 診斷 Debug（新面板）
- SSE 連線狀態、心跳秒數（逾時警示）、行情速率、訂閱數、API 位址、伺服器版本／模式、token 有效時數、合約數、最近委託事件原始內容
- 行情不動時不用開 devtools 就能判斷狀況

### 視覺與體驗
- **資產分布**改為環形圖：中心顯示資產市值總額、對齊的明細列與前五大持股佔比
- **啟動中／連線中**改為精緻的旋轉弧環動畫＋流光進度條，啟動過程不再像卡住
- 移除上方列的 Contracts 數字（移至診斷面板）

---

Shioaji Pro 桌面版 — 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` ｜ Windows `.msi` / `.exe` ｜ Linux `.AppImage` / `.deb` / `.rpm`
